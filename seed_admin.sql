-- ============================================================
-- RBZ SYSTEM — BOOTSTRAP SENIOR BE ADMIN USER
-- Run this ONCE on a fresh database.
-- Replace the BCrypt hash below with a real one generated by:
--   htpasswd -nbBC 12 "" YourPassword | tr -d ':\n' | sed 's/$apr1/$2y/'
-- OR use the /api/examiners endpoint (POST) while the app is running
--   which handles hashing automatically.
--
-- Easiest approach: start the app and POST to /api/examiners:
-- curl -X POST http://localhost:8080/api/examiners \
--   -H "Content-Type: application/json" \
--   -d '{
--     "fullName": "Senior BE Admin",
--     "username": "admin_sbe",
--     "password": "ChangeMe2026!",
--     "email": "sbe@rbz.co.zw",
--     "employeeId": "SBE-2026-001",
--     "designation": "Deputy Director - Bank Supervision",
--     "role": "SENIOR_BE"
--   }'
-- ============================================================
